spring:
  application:
    name: site-backend

  datasource:
    url: jdbc:mysql://localhost:3306/site_cerfox?useSSL=false&allowPublicKeyRetrieval=true
    username: cerf_admin
    password: HMite/!_6qc=YwW
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    show-sql: true

    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration